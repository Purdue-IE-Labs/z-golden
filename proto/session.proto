syntax = "proto3";

import "base_data.proto";

// Published on /DATA keys
message TagUpdate {
    map<int32, BaseData> tags = 1; // maps alias to BaseData
    fixed64 meta_ts = 2;
}

// Sent to queryable declared on /METHODS/.. 
message MethodCall {
    map<int32, BaseData> params = 1;
    
}

